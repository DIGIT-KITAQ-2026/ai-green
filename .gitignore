node_modules/
.next/
out/
# next dev と next build で中身が入れ替わる生成ファイル。
# 追跡すると、実行したコマンド次第で毎回差分が出て pull がぶつかる。
next-env.d.ts
# .env は追跡している（公開して安全な NEXT_PUBLIC_ の値だけを置く）。
# 秘密（SUPABASE_SERVICE_ROLE_KEY）は .env.local に書くこと。
.env.local
.env.local.*
.env*.local
*.log
.DS_Store
*.tsbuildinfo
